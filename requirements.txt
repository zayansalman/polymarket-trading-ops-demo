# Python 3.11 target (matches HF Space Docker SDK default).
# Deps are added per build step to avoid installation friction.

# --- Foundation: local dashboard + APIs + SQLite ---
httpx==0.27.2
aiosqlite==0.20.0
python-dotenv==1.0.1
structlog==24.4.0
apscheduler==3.10.4
pydantic==2.9.2
gradio==5.49.1

# --- HF-only LLM helpers for weather analysis ---
huggingface-hub==0.35.3
json-repair==0.30.3
aiohttp==3.10.10                # required by huggingface_hub.AsyncInferenceClient

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# BTC live automation dependencies are intentionally not listed yet.
